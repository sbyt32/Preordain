name: Lint

on:
  pull_request_target:
    branches: master
  push:
    branches: master
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: psf/black@stable
